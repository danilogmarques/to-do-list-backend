export type TuserDB = {
    id: string,
    name: string,
    email: string,
    password: string,
}

export type TTaskDB = {
    id: string,
    title: string,
    description: string,
    creat_at: string,
    status: number
}